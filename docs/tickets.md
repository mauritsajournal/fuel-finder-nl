# Fuel Finder NL -- Ticket Backlog

> Generated by Software Architect Agent
> Date: 2026-03-13
> Based on: technical-blueprint.md (post-verification revision)

---

## [x] T-001 -- Project scaffolding
> DONE 2026-03-13 -- SvelteKit 2 + Svelte 5 + Tailwind 4 + Vitest + ESLint + Prettier. Build/test/typecheck pass.

**Phase:** 1 (MVP)
**Component:** Frontend
**Depends on:** None
**Estimated effort:** S
**Priority:** P0 (critical path)

**Description:**
Initialize the SvelteKit project with static adapter for GitHub Pages deployment. Set up Tailwind CSS 4, TypeScript, and all dev tooling.

**Acceptance criteria:**
- [ ] SvelteKit 2.x project created with `adapter-static`
- [ ] Svelte 5 with runes enabled
- [ ] Tailwind CSS 4 configured with dark theme defaults
- [ ] TypeScript strict mode enabled
- [ ] ESLint + Prettier configured with Svelte plugins
- [ ] Vitest configured for unit tests
- [ ] pnpm as package manager
- [ ] `.gitignore` includes node_modules, build output, .env
- [ ] `pnpm dev` starts local dev server successfully
- [ ] `pnpm build` produces static output in `build/` directory

**Technical notes:**
Use `npx sv create` to scaffold. Add `@sveltejs/adapter-static` and configure `svelte.config.js` with `fallback: '404.html'` and `paths.base` for GitHub Pages subdomain. Reference: technical-blueprint.md Section 2.1.

Install: `@sveltejs/adapter-static`, `tailwindcss`, `@tailwindcss/vite`, `maplibre-gl` (added in T-005).

**Files likely affected:**
- `svelte.config.js`
- `src/app.html`
- `src/app.css`
- `src/routes/+layout.svelte`
- `src/routes/+page.svelte`
- `tailwind.config.js` or `@import` in CSS
- `vite.config.ts`
- `tsconfig.json`
- `.eslintrc.cjs`
- `.prettierrc`
- `package.json`

---

## [x] T-002 -- GitHub Pages deployment pipeline
> DONE 2026-03-13 -- deploy.yml with pnpm/Node 22, GitHub Pages enabled via API.

**Phase:** 1 (MVP)
**Component:** CI/CD
**Depends on:** T-001
**Estimated effort:** S
**Priority:** P0 (critical path)

**Description:**
Set up GitHub Actions workflow to build the SvelteKit static site and deploy to GitHub Pages on push to main.

**Acceptance criteria:**
- [ ] `.github/workflows/deploy.yml` created
- [ ] Workflow triggers on push to `main` and `workflow_dispatch`
- [ ] Builds SvelteKit with `pnpm build`
- [ ] Deploys build output to GitHub Pages
- [ ] Site is accessible at `https://mauritsajournal.github.io/fuel-finder-nl/`
- [ ] Workflow completes in under 3 minutes

**Technical notes:**
Use `actions/deploy-pages@v4` with `actions/upload-pages-artifact`. Set `pages` permission in workflow. Reference: FPL Draft Dashboard deploy workflow for proven pattern. Remember to set `paths.base` in `svelte.config.js` to `/fuel-finder-nl`.

**Files likely affected:**
- `.github/workflows/deploy.yml`
- `svelte.config.js` (paths.base)

---

## [x] T-003 -- Cloudflare R2 bucket setup and upload tooling
> DONE (code) 2026-03-13 -- upload_to_r2.py written. R2 bucket creation blocked on H-001.

**Phase:** 1 (MVP)
**Component:** Infrastructure
**Depends on:** None
**Estimated effort:** M
**Priority:** P0 (critical path)

**Description:**
Create a Cloudflare R2 bucket for storing tiled data files. Set up public read access. Create a Python utility script for uploading JSON tiles to R2 from GitHub Actions. Configure CORS on the R2 bucket to allow requests from the GitHub Pages domain.

**Acceptance criteria:**
- [ ] R2 bucket `fuel-finder-data` created in Cloudflare account
- [ ] Public read access enabled on the bucket
- [ ] CORS configured to allow `https://mauritsajournal.github.io` origin
- [ ] Python upload script (`scripts/upload_to_r2.py`) works with boto3 S3-compatible API
- [ ] Script uploads files to R2 and sets appropriate `Content-Type: application/json` and `Cache-Control: public, max-age=3600`
- [ ] GitHub Secrets configured: `R2_ACCESS_KEY_ID`, `R2_SECRET_ACCESS_KEY`, `R2_ENDPOINT`, `R2_BUCKET_NAME`
- [ ] Test upload of a sample `metadata.json` succeeds
- [ ] File is accessible via public R2 URL from browser

**Technical notes:**
Use `boto3` with S3-compatible endpoint for R2. Cloudflare R2 free tier: 10GB storage, 10M class B reads/month, 1M class A writes/month. The R2 endpoint format is `https://{account_id}.r2.cloudflarestorage.com`. Public access can be enabled via Cloudflare dashboard or API. Reference: technical-blueprint.md Section 5.6.

**Files likely affected:**
- `scripts/upload_to_r2.py`
- `scripts/requirements.txt`
- `.github/workflows/data-pipeline.yml` (partially, secrets reference)

---

## [x] T-004 -- ANWB fuel prices data pipeline
> DONE (code) 2026-03-13 -- fetch_fuel_prices.py + tile_data.py + data-pipeline.yml. Needs R2 for upload step.

**Phase:** 1 (MVP)
**Component:** Data Pipeline
**Depends on:** T-003
**Estimated effort:** L
**Priority:** P0 (critical path)

**Description:**
Create a Python script that fetches fuel station data from the ANWB API, processes it into the FuelStation data model, splits into geographic tiles, and uploads to Cloudflare R2. Create the GitHub Actions workflow to run this on a schedule.

**Acceptance criteria:**
- [ ] Python script `scripts/fetch_fuel_prices.py` fetches from ANWB API
- [ ] Covers all of Netherlands using ~6 bounding box queries (geobox limitation)
- [ ] 1-second delay between API requests
- [ ] Deduplicates stations that appear in multiple bounding boxes
- [ ] Normalizes prices to EUR/liter with 3 decimal places
- [ ] Validates: each station has id, lat, lng, at least one price
- [ ] Outputs data matching FuelStation data model from blueprint
- [ ] Python script `scripts/tile_data.py` splits stations into 0.5-degree grid tiles
- [ ] Generates `metadata.json` with last_updated timestamp, station count, tile index
- [ ] Uploads tiles + metadata to R2 via `upload_to_r2.py`
- [ ] GitHub Actions workflow `data-pipeline.yml` runs every 4 hours + manual trigger
- [ ] Workflow handles API failures gracefully (retries 3x, keeps previous data)
- [ ] At least 3,000 stations fetched (ANWB covers ~3,800)

**Technical notes:**
ANWB API endpoint: `https://api.anwb.nl/routing/points-of-interest/v3/all?type-filter=FUEL_STATION&bounding-box-filter={south},{west},{north},{east}`. No auth needed. Response is JSON with station objects containing `id`, `title`, `address`, `coordinates`, `prices` array. Each price has `fuelType` and `value`.

NL bounding box: 50.7-53.6N, 3.3-7.3E. Split into ~6 overlapping geoboxes. Deduplicate by station `id`.

Reference: `pyfuelprices` library `anwbonderweg.py` for working implementation. Reference: technical-blueprint.md Section 5.1.

**Files likely affected:**
- `scripts/fetch_fuel_prices.py`
- `scripts/tile_data.py`
- `scripts/upload_to_r2.py` (from T-003)
- `scripts/requirements.txt`
- `.github/workflows/data-pipeline.yml`

---

## [x] T-005 -- MapLibre map component with dark theme
> DONE 2026-03-13 -- MapLibre 5.x with custom dark style, OpenFreeMap tiles, NL-bounded.

**Phase:** 1 (MVP)
**Component:** Frontend - Map
**Depends on:** T-001
**Estimated effort:** L
**Priority:** P0 (critical path)

**Description:**
Create the core interactive map component using MapLibre GL JS with OpenFreeMap vector tiles. Apply a custom dark theme style that complements the glassmorphism UI. The map should fill the viewport and support smooth pan/zoom on mobile.

**Acceptance criteria:**
- [ ] MapLibre GL JS 4.x installed and rendering in a Svelte component
- [ ] Uses OpenFreeMap vector tiles (no API key needed)
- [ ] Custom dark theme map style applied (dark background, muted road colors, light labels)
- [ ] Map fills entire viewport (mobile-first, full-bleed)
- [ ] Smooth 60fps pan and zoom on mobile (Chrome DevTools test)
- [ ] Map centered on Netherlands on load (center: [5.2913, 52.1326], zoom: 7)
- [ ] Attribution for OpenStreetMap displayed
- [ ] MapLibre CSS imported correctly (prevents unstyled flash)
- [ ] Component exports map instance for other components to interact with

**Technical notes:**
MapLibre GL JS needs dynamic import in SvelteKit (SSR window issue). Use `onMount` or `{#if browser}` guard. OpenFreeMap style URL: `https://tiles.openfreemap.org/styles/liberty` -- this is a light style. For dark theme, either use a community dark style (e.g., `positron` dark variant) or create a custom JSON style. Creating a full custom style is significant effort; start with an existing dark basemap and tweak. Consider MapTiler's dark style as fallback reference.

Reference: technical-blueprint.md Section 3.2.

**Files likely affected:**
- `src/lib/components/Map.svelte`
- `src/lib/styles/map-dark-style.json` (custom MapLibre style)
- `src/routes/+page.svelte`
- `package.json` (maplibre-gl dependency)

---

## [x] T-006 -- Viewport-based tile loading from R2
> DONE 2026-03-13 -- Data store, tile cache, debounced loading, metadata fetch.

**Phase:** 1 (MVP)
**Component:** Frontend - Data Layer
**Depends on:** T-003, T-004, T-005
**Estimated effort:** M
**Priority:** P0 (critical path)

**Description:**
Implement the data store that loads tiled JSON data from Cloudflare R2 based on the current map viewport. On map move, determine which tiles are visible, fetch any not yet loaded, and provide reactive data to other components.

**Acceptance criteria:**
- [ ] Fetches `metadata.json` from R2 on app load
- [ ] Calculates visible tiles based on map viewport bounds
- [ ] Fetches tile JSON files from R2 for visible tiles
- [ ] Caches loaded tiles in memory (never re-fetches a loaded tile within session)
- [ ] Debounces tile loading on map `moveend` event (300ms)
- [ ] Provides reactive Svelte store with all currently loaded stations
- [ ] Converts station data to GeoJSON for MapLibre source
- [ ] Shows loading indicator while fetching tiles
- [ ] Handles fetch errors gracefully (skip failed tile, retry once)
- [ ] Shows "last updated X hours ago" from metadata timestamps

**Technical notes:**
Use Svelte 5 `$state` rune for the tile cache. MapLibre `getCenter()` and `getBounds()` give viewport info. Calculate tile keys from bounds (floor to 0.5-degree grid). R2 public URL pattern: `https://pub-{hash}.r2.dev/tiles/fuel/{lat}_{lng}.json`. Set the R2 base URL via environment variable (`PUBLIC_R2_URL`).

Reference: technical-blueprint.md Section 3.6 and 4.2.

**Files likely affected:**
- `src/lib/stores/data.svelte.ts`
- `src/lib/utils/tiles.ts`
- `src/lib/types.ts` (FuelStation, EVCharger, POI, Metadata types)
- `.env.example` (PUBLIC_R2_URL)

---

## [x] T-007 -- Fuel station markers with price color-coding
> DONE 2026-03-13 -- Price thresholds, color categories, format utils, 22 tests.

**Phase:** 1 (MVP)
**Component:** Frontend - Map
**Depends on:** T-005, T-006
**Estimated effort:** M
**Priority:** P0 (critical path)

**Description:**
Render fuel station markers on the map using MapLibre GeoJSON source and layers. Color-code markers by price (green = cheap, yellow = average, red = expensive). Implement marker clustering at low zoom levels to prevent visual clutter.

**Acceptance criteria:**
- [ ] Fuel stations rendered as circular markers on the map
- [ ] Color scale: green (#22c55e) for cheapest 25%, yellow (#eab308) for middle 50%, red (#ef4444) for most expensive 25%
- [ ] Price thresholds calculated dynamically from loaded data (not hardcoded)
- [ ] Markers show fuel brand icon or letter when zoomed in
- [ ] Marker clustering enabled at zoom levels < 10
- [ ] Cluster circles show count number
- [ ] Clicking a cluster zooms into it
- [ ] Clicking a station marker triggers station selection (for detail panel)
- [ ] Markers update reactively when data store changes (new tiles loaded)

**Technical notes:**
Use MapLibre `addSource` with `type: 'geojson'` and `cluster: true`. Use `addLayer` with `type: 'circle'` and data-driven `circle-color` based on a `priceCategory` property. Add the price category during GeoJSON conversion in the data store. Default fuel type for color-coding: Euro 95 (user can change via filters later).

Reference: technical-blueprint.md Section 3.2.

**Files likely affected:**
- `src/lib/components/FuelMarkers.svelte` (or integrated in Map.svelte)
- `src/lib/utils/price-colors.ts`
- `src/lib/stores/data.svelte.ts` (GeoJSON conversion with priceCategory)

---

## [x] T-008 -- Station detail slide-up panel
> DONE 2026-03-13 -- Glassmorphism panel with prices, address, facilities, navigation links.

**Phase:** 1 (MVP)
**Component:** Frontend - UI
**Depends on:** T-007
**Estimated effort:** M
**Priority:** P0 (critical path)

**Description:**
Create a slide-up bottom panel that appears when a fuel station marker is tapped. Shows all station details: prices, address, brand, facilities. Includes action buttons for navigation.

**Acceptance criteria:**
- [ ] Panel slides up from bottom of screen with smooth CSS transition
- [ ] Shows station name and brand with brand-colored header
- [ ] Displays all available fuel prices with per-type labels
- [ ] Shows price relative indicator: "X.XXX (Y cent goedkoper dan gemiddeld)" or similar
- [ ] Shows address (street, city, postal code)
- [ ] Shows distance from user (if GPS available, straight-line initially)
- [ ] Shows facilities icons if available (toilet, shop, car wash)
- [ ] "Navigate" button (triggers route calculation in T-013)
- [ ] "Open in Google Maps" external link button
- [ ] "Open in Waze" external link button
- [ ] Panel can be dismissed by swiping down or tapping backdrop
- [ ] Panel is glassmorphism styled (backdrop-blur, semi-transparent bg)

**Technical notes:**
Google Maps external link: `https://www.google.com/maps/dir/?api=1&destination={lat},{lng}`. Waze: `https://waze.com/ul?ll={lat},{lng}&navigate=yes`. Use Tailwind's `backdrop-blur-lg` and `bg-gray-900/80` for glassmorphism effect. Animate with CSS `transform: translateY()` transition.

Reference: technical-blueprint.md Section 3.3.

**Files likely affected:**
- `src/lib/components/StationPanel.svelte`
- `src/lib/stores/selection.svelte.ts` (selected station state)

---

## [x] T-009 -- GPS-based current location
> DONE 2026-03-13 -- LocateButton + location store, flyTo, pulsing blue dot, GDPR-compliant.

**Phase:** 1 (MVP)
**Component:** Frontend - Map
**Depends on:** T-005
**Estimated effort:** S
**Priority:** P1 (important)

**Description:**
Implement a "Find me" button that uses the browser Geolocation API to get the user's current position and fly the map to that location. Show a user location marker on the map.

**Acceptance criteria:**
- [ ] "Find me" / GPS button in UI (bottom-right corner, glassmorphism styled)
- [ ] Clicking triggers `navigator.geolocation.getCurrentPosition()`
- [ ] GDPR-compliant: permission requested only on button click, not on page load
- [ ] Map flies to user location with smooth animation (zoom level 12)
- [ ] User location shown as a pulsing blue dot on the map
- [ ] Error handling: show toast if user denies permission or GPS fails
- [ ] Position stored in a Svelte store for distance calculations

**Technical notes:**
Use MapLibre's built-in `GeolocateControl` or custom implementation. For glassmorphism styling, custom is better. Geolocation API returns `GeolocationPosition` with `coords.latitude` and `coords.longitude`. Use `map.flyTo()` for smooth transition.

**Files likely affected:**
- `src/lib/components/LocateButton.svelte`
- `src/lib/stores/location.svelte.ts`
- `src/lib/components/Map.svelte` (user marker layer)

---

## [x] T-010 -- Glassmorphism dark theme and layout
> DONE 2026-03-13 -- Full glass UI: TopBar with search, FilterBar with fuel toggles, dark MapLibre controls, responsive.

**Phase:** 1 (MVP)
**Component:** Frontend - UI/UX
**Depends on:** T-001
**Estimated effort:** M
**Priority:** P0 (critical path)

**Description:**
Design and implement the overall glassmorphism dark theme. Create the app layout with the map as full-screen background, glass-styled overlay panels for controls, and smooth visual transitions.

**Acceptance criteria:**
- [ ] Dark color palette defined (bg: #0f172a or similar dark navy, accents: blues/greens)
- [ ] Glassmorphism CSS utilities: `backdrop-blur-lg`, `bg-white/5`, `border border-white/10`, `shadow-xl`
- [ ] App layout: full-screen map with floating glass panels for controls
- [ ] Top bar: app name + search input (glass-styled)
- [ ] Bottom bar: filter toggles (fuel type selector)
- [ ] All interactive elements have hover/active states
- [ ] Smooth transitions on panel open/close (200-300ms ease)
- [ ] Custom color scheme for dark map tiles that harmonizes with the UI
- [ ] Font: Inter or system font stack (clean, modern)
- [ ] Responsive: works on 360px-width screens up to desktop
- [ ] Loading skeleton states (pulsing glass rectangles)

**Technical notes:**
Tailwind 4 supports `backdrop-blur` natively. Key CSS for glassmorphism:
```css
.glass {
  background: rgba(255, 255, 255, 0.05);
  backdrop-filter: blur(16px);
  border: 1px solid rgba(255, 255, 255, 0.1);
  box-shadow: 0 8px 32px rgba(0, 0, 0, 0.3);
}
```
Test on both light and dark map backgrounds (map style may vary at zoom levels).

**Files likely affected:**
- `src/app.css` (global styles, Tailwind config)
- `src/routes/+layout.svelte` (app shell)
- `src/routes/+page.svelte` (main layout)
- `src/lib/components/TopBar.svelte`
- `src/lib/components/FilterBar.svelte`

---

## [ ] T-011 -- OpenChargeMap EV charger data pipeline

**Phase:** 2 (Full Data)
**Component:** Data Pipeline
**Depends on:** T-003, T-004
**Estimated effort:** M
**Priority:** P1 (important)

**Description:**
Add EV charging station data from OpenChargeMap to the data pipeline. Fetch NL data, process into EVCharger model, split into tiles, upload to R2 alongside fuel data.

**Acceptance criteria:**
- [ ] Python script `scripts/fetch_ev_chargers.py` fetches from OpenChargeMap API
- [ ] Uses API key from GitHub Secrets (`OCM_API_KEY`)
- [ ] Fetches all NL chargers (countrycode=NL, compact format)
- [ ] Extracts: location, operator, connector types, power levels, pricing (if available), status
- [ ] Outputs data matching EVCharger data model
- [ ] Tiles data using same 0.5-degree grid as fuel stations
- [ ] Uploads EV tiles to `tiles/ev/` path in R2
- [ ] Updates metadata.json with EV-specific timestamps and counts
- [ ] Integrated into `data-pipeline.yml` workflow
- [ ] At least 20,000 charging points fetched

**Technical notes:**
OpenChargeMap API endpoint: `https://api.openchargemap.io/v3/poi/?output=json&countrycode=NL&maxresults=10000&compact=true&verbose=false&key={KEY}`. May need multiple requests with `offset` if >10,000 results. API key required for >250 results -- register at openchargemap.org. Pricing data is in `UsageCost` field (free text, needs parsing). Connection status in `StatusType`. Reference: technical-blueprint.md Section 5.3.

**Files likely affected:**
- `scripts/fetch_ev_chargers.py`
- `scripts/tile_data.py` (extend for EV data type)
- `.github/workflows/data-pipeline.yml`

---

## [ ] T-012 -- Overpass API POI data pipeline

**Phase:** 2 (Full Data)
**Component:** Data Pipeline
**Depends on:** T-003
**Estimated effort:** S
**Priority:** P1 (important)

**Description:**
Fetch Points of Interest (public toilets, rest areas, highway services) from OpenStreetMap via the Overpass API. Process into POI model, tile, and upload to R2.

**Acceptance criteria:**
- [ ] Python script `scripts/fetch_poi.py` queries Overpass API
- [ ] Fetches: amenity=toilets, highway=rest_area, highway=services near major roads
- [ ] Scopes playgrounds to within 500m of fuel stations or highway exits (prevents noise)
- [ ] Outputs data matching POI data model
- [ ] Tiles using same grid as other data
- [ ] Uploads to `tiles/poi/` in R2
- [ ] Updates metadata.json
- [ ] Runs daily (not every 4h -- POI data changes rarely)
- [ ] Handles Overpass API timeouts gracefully (60s timeout, 1 retry)
- [ ] Workflow updated to run POI fetch daily only

**Technical notes:**
Overpass API endpoint: `https://overpass-api.de/api/interpreter` (POST). Query NL by `area["ISO3166-1"="NL"]`. Important: Overpass is a shared public resource -- be respectful. Max 2 queries per cycle. Set 60s timeout. Don't fetch ALL playgrounds in NL (~15,000) -- scope to driver-relevant locations only.

Reference: technical-blueprint.md Section 5.4.

**Files likely affected:**
- `scripts/fetch_poi.py`
- `scripts/tile_data.py`
- `.github/workflows/data-pipeline.yml`

---

## [x] T-013 -- Cloudflare Worker routing proxy
> DONE (code) 2026-03-13 -- Worker source with /route, /geocode, rate limiting, CORS. Deploy blocked on H-001/H-002.

**Phase:** 2 (Full Data)
**Component:** Infrastructure
**Depends on:** None
**Estimated effort:** M
**Priority:** P0 (critical path)

**Description:**
Create a Cloudflare Worker that acts as a proxy for OpenRouteService API requests. The Worker adds the ORS API key server-side, validates requests, and enforces per-IP rate limiting. This prevents API key exposure in client-side JavaScript.

**Acceptance criteria:**
- [ ] Cloudflare Worker `routing-proxy` created and deployed
- [ ] `/route` endpoint: proxies to ORS Directions API
- [ ] `/geocode` endpoint: proxies to ORS Geocode API
- [ ] Worker adds ORS API key from environment variable (never in client code)
- [ ] Validates: start/end coordinates within NL bounding box (50.5-53.7N, 3.2-7.4E)
- [ ] Per-IP rate limiting: max 10 requests/minute (return 429 if exceeded)
- [ ] CORS headers: allow `https://mauritsajournal.github.io` and `http://localhost:*`
- [ ] Returns proper error codes: 400 (bad request), 429 (rate limit), 502 (ORS error)
- [ ] Deployed at `https://fuel-finder-routing.{account}.workers.dev`
- [ ] Worker environment variable `ORS_API_KEY` set via `wrangler secret put`

**Technical notes:**
Use `wrangler` CLI to create and deploy. Worker is JavaScript/TypeScript. Rate limiting options: use Cloudflare's built-in rate limiting (paid) or implement simple in-memory counter (resets on Worker restart, acceptable for PoC). For robust per-IP limiting, use Workers KV as a counter store. ORS free tier: 2,000 requests/day, 40/minute. Our Worker rate limit (10/min/IP) is more conservative to prevent single-user abuse.

```javascript
// Pseudocode for Worker
export default {
  async fetch(request, env) {
    const url = new URL(request.url);
    if (url.pathname === '/route') {
      // validate coords, add API key, proxy to ORS directions
    } else if (url.pathname === '/geocode') {
      // sanitize text, add API key, proxy to ORS geocode
    }
  }
};
```

**Files likely affected:**
- `worker/src/index.ts` (Worker source)
- `worker/wrangler.toml` (Worker config)
- `worker/package.json`

---

## [ ] T-014 -- EV charger markers on map

**Phase:** 2 (Full Data)
**Component:** Frontend - Map
**Depends on:** T-006, T-011
**Estimated effort:** M
**Priority:** P1 (important)

**Description:**
Add EV charging station markers to the map. Show connector type icons, power levels, and operator info. Different marker style from fuel stations to avoid confusion.

**Acceptance criteria:**
- [ ] EV chargers rendered as distinct markers (different shape/icon from fuel, e.g., lightning bolt)
- [ ] Marker color indicates charger type: green for fast (>50kW), blue for regular (<50kW)
- [ ] Clustering at low zoom levels
- [ ] Clicking shows a detail popup/panel with: operator, connector types, power levels, pricing (if available)
- [ ] Toggle to show/hide EV layer (tied to filter bar fuel type selector)
- [ ] Loads EV tile data from R2 using same viewport-based approach

**Technical notes:**
Reuse tile loading logic from T-006. Add EV as a second data type in the store. Use separate MapLibre source and layers for EV data. Consider using MapLibre symbols (icon images) for distinct marker shapes. Connector types: CCS, CHAdeMO, Type 2 -- map to readable labels.

**Files likely affected:**
- `src/lib/components/EVMarkers.svelte`
- `src/lib/stores/data.svelte.ts` (extend for EV tiles)
- `src/lib/types.ts` (EVCharger type)

---

## [ ] T-015 -- POI markers on map

**Phase:** 2 (Full Data)
**Component:** Frontend - Map
**Depends on:** T-006, T-012
**Estimated effort:** S
**Priority:** P2 (nice to have)

**Description:**
Add POI markers (toilets, rest areas, playgrounds) to the map with appropriate icons. These should be visually subdued compared to fuel/EV markers.

**Acceptance criteria:**
- [ ] POI markers rendered with small, muted icons per type
- [ ] Toilet: WC icon, Rest area: bench/park icon, Playground: swing icon
- [ ] Markers are smaller and more transparent than fuel/EV markers
- [ ] Toggle to show/hide POI layer
- [ ] Clicking shows simple popup with name/type
- [ ] Loads POI tile data from R2

**Technical notes:**
POIs are supplementary data -- they should not dominate the map visually. Use small (12px) markers with 60% opacity. Only show at zoom > 10 to avoid clutter at country level.

**Files likely affected:**
- `src/lib/components/POIMarkers.svelte`
- `src/lib/stores/data.svelte.ts` (extend for POI tiles)
- `static/icons/` (SVG icons for POI types)

---

## [ ] T-016 -- Route calculation via Worker proxy

**Phase:** 2 (Full Data)
**Component:** Frontend - Routing
**Depends on:** T-008, T-009, T-013
**Estimated effort:** M
**Priority:** P1 (important)

**Description:**
Implement route calculation from user location to a selected station. Show the route as a polyline on the map with driving time and distance. Uses the Cloudflare Worker proxy for ORS API calls.

**Acceptance criteria:**
- [ ] "Navigate" button in station detail panel triggers route calculation
- [ ] Calls Worker `/route` endpoint with start (user location) and end (station coords)
- [ ] Displays route as a colored polyline on the map (blue, 4px width)
- [ ] Shows driving time (e.g., "12 min") and distance (e.g., "8.3 km") in the panel
- [ ] Route clears when panel is closed or new station selected
- [ ] Debounced: no duplicate requests within 500ms
- [ ] Caches route results in sessionStorage (key: start_end coords, 1hr TTL)
- [ ] Fallback: if Worker/ORS fails, show straight-line distance with estimated time (distance/60km/h)
- [ ] Loading state shown while route calculates

**Technical notes:**
ORS returns GeoJSON geometry. Add as a MapLibre GeoJSON source with a line layer. Duration is in seconds, distance in meters -- convert for display. The Worker endpoint is configured in T-013. Set Worker URL via environment variable `PUBLIC_WORKER_URL`.

**Files likely affected:**
- `src/lib/services/routing.ts`
- `src/lib/components/StationPanel.svelte` (integrate route button)
- `src/lib/components/Map.svelte` (route layer)
- `.env.example` (PUBLIC_WORKER_URL)

---

## [x] T-017 -- Filter by fuel type and sorting
> DONE 2026-03-13 -- Shared filters store, fuel type toggle updates price comparisons, sort toggle (cheapest/nearest) with GPS.

**Phase:** 2 (Full Data)
**Component:** Frontend - UI
**Depends on:** T-007
**Estimated effort:** M
**Priority:** P1 (important)

**Description:**
Implement filter controls allowing users to switch between fuel types (Euro 95, Diesel, LPG, Electric) and sort stations by price or distance.

**Acceptance criteria:**
- [ ] Bottom filter bar with fuel type toggle buttons: Euro 95 | Diesel | LPG | Electric
- [ ] Selecting "Electric" shows EV markers and hides fuel markers (and vice versa)
- [ ] Price color-coding recalculates based on selected fuel type
- [ ] Sort options accessible from filter bar: "Cheapest" | "Nearest" (if GPS available)
- [ ] Filter state persisted in URL query params (shareable links)
- [ ] Glassmorphism styled filter bar
- [ ] Active filter button visually highlighted

**Technical notes:**
Use Svelte 5 `$state` for filter state. When fuel type changes, update the MapLibre paint properties for color-coding (data-driven styling). For "Nearest" sort, calculate distances from user position to all loaded stations. Use URL `searchParams` for state persistence.

**Files likely affected:**
- `src/lib/components/FilterBar.svelte`
- `src/lib/stores/filters.svelte.ts`
- `src/lib/utils/price-colors.ts` (update for multi-fuel)

---

## [ ] T-018 -- Address/city search with geocoding

**Phase:** 2 (Full Data)
**Component:** Frontend - UI
**Depends on:** T-013
**Estimated effort:** S
**Priority:** P1 (important)

**Description:**
Add a search input that allows users to search by address or city name. Uses the Cloudflare Worker proxy for ORS geocoding. Flies map to the selected result.

**Acceptance criteria:**
- [ ] Search input in top bar with glassmorphism styling
- [ ] Debounced input (300ms) sends query to Worker `/geocode` endpoint
- [ ] Dropdown shows up to 5 results with place name and region
- [ ] Selecting a result flies the map to that location
- [ ] Search results scoped to Netherlands (Worker adds boundary.country=NL)
- [ ] Clear button to reset search
- [ ] Accessible: keyboard navigation works in dropdown, proper ARIA labels

**Technical notes:**
ORS Geocode API returns GeoJSON FeatureCollection with features containing `properties.label` (display name) and `geometry.coordinates`. Use `map.flyTo()` with the selected coordinates.

**Files likely affected:**
- `src/lib/components/SearchBar.svelte`
- `src/lib/services/geocoding.ts`

---

## [x] T-019 -- Data health indicator
> DONE 2026-03-13 -- DataHealth component with freshness colors, expandable status panel, Dutch text.

**Phase:** 3 (Polish)
**Component:** Frontend - UI
**Depends on:** T-006
**Estimated effort:** S
**Priority:** P2 (nice to have)

**Description:**
Show data freshness indicators in the UI so users know when prices were last updated. Include a "data status" view showing health of each data source.

**Acceptance criteria:**
- [ ] Small text in bottom-left corner: "Prices updated X hours ago"
- [ ] Text turns orange if >8 hours old, red if >24 hours old
- [ ] Tapping the text opens a "data health" mini-panel showing:
  - Last fuel price update
  - Last EV charger update
  - Last POI update
  - Number of stations/chargers loaded
- [ ] Panel is glassmorphism styled

**Technical notes:**
Read timestamps from `metadata.json`. Use relative time formatting (Intl.RelativeTimeFormat or simple utility function).

**Files likely affected:**
- `src/lib/components/DataHealth.svelte`

---

## [x] T-020 -- PWA manifest and service worker
> DONE 2026-03-13 -- Manifest, SVG icons (192/512/maskable), service worker with cache-first app shell and network-first data tiles.

**Phase:** 3 (Polish)
**Component:** Frontend - PWA
**Depends on:** T-001
**Estimated effort:** M
**Priority:** P2 (nice to have)

**Description:**
Make the app installable as a Progressive Web App. Add a web app manifest and service worker that caches the app shell for offline startup.

**Acceptance criteria:**
- [ ] `static/manifest.json` with app name, icons, theme color, display: standalone
- [ ] App icons generated (192x192, 512x512 PNG)
- [ ] Service worker caches: HTML, JS, CSS bundles (app shell)
- [ ] Service worker caches last-viewed data tiles for offline viewing
- [ ] Offline: app opens with cached data, shows "offline" badge, routing disabled
- [ ] "Install app" prompt handled gracefully
- [ ] `<meta>` tags for theme-color, apple-mobile-web-app-capable
- [ ] Passes basic Lighthouse PWA audit

**Technical notes:**
SvelteKit has `@vite-pwa/sveltekit` plugin, or manual service worker setup. For PoC, a simple cache-first service worker for static assets + network-first for data tiles is sufficient. Don't over-engineer the offline strategy -- cache app shell + last data is enough for "installable on home screen" demo.

**Files likely affected:**
- `static/manifest.json`
- `static/icons/` (app icons)
- `src/service-worker.ts` (or via vite-plugin-pwa)
- `src/app.html` (manifest link, meta tags)

---

## [ ] T-021 -- Performance optimization and polish

**Phase:** 3 (Polish)
**Component:** Frontend
**Depends on:** All Phase 1 + 2 tickets
**Estimated effort:** M
**Priority:** P1 (important)

**Description:**
Final pass on performance, animations, error handling, and cross-browser testing to make the PoC demo-ready.

**Acceptance criteria:**
- [ ] First contentful paint < 3 seconds on 4G (Chrome DevTools throttle)
- [ ] Smooth fly-to animations when selecting stations
- [ ] All error states handled with user-friendly toast messages
- [ ] Loading skeleton states for tiles, station panel, route
- [ ] No console errors in Chrome, Safari, Firefox mobile
- [ ] Touch interactions work correctly (tap, pinch-zoom, swipe panel)
- [ ] Tested on real mobile device (or BrowserStack)
- [ ] Lighthouse performance score > 80
- [ ] Simple privacy notice accessible from the app (what data is used, no tracking)

**Technical notes:**
Key performance wins: preconnect to OpenFreeMap tile server, lazy load non-critical components, use MapLibre `rasterize` event for loading indicators, minimize re-renders with Svelte 5 fine-grained reactivity.

**Files likely affected:**
- Various components (polish pass)
- `src/app.html` (preconnect hints)
- `src/lib/components/Toast.svelte`
- `src/routes/privacy/+page.svelte` (privacy notice)

---
